# 1.2 Patch

## 목표

- 대상: 에이든 시뮬레이터, 든뮬레이터 1.2
- 목적: 아이템·특성 추가 효과 고려
- UI: `[추가 효과]` 버튼, 추가 효과 설정 창
- 적용 방식: 토글 ON/OFF, 시뮬레이터 전투 로직 반영

## 추가 효과

### 의념

- 분류: 고유 장착 효과
- 조건: 스킬 사용
- 상태: 3초 안에 다음 기본 공격 강화
- 추가 피해: 근거리 대상 최대 체력 12%, 원거리 대상 최대 체력 11%
- 쿨다운: 2초
- 구현: `willpower.armed`, `willpower.cooldown`, `applyBasicAttackEffects`

### 충전 - 섬광

- 분류: 고유 장착 효과
- 조건: 다음 기본 공격 적중
- 추가 피해: 캐릭터 레벨 * 11
- 둔화: 1.5초, 이동 속도 20% 감소
- 쿨다운: 5초
- 구현: `flash.cooldown`, `dummy.flashSlow`, `characterLevel`

### 뇌명 집행

- 분류: 고유 장착 효과
- 조건: 5초 안에 기본 공격 또는 개별 스킬 2회 적중
- 상태: 8초, 방어 관통 +12
- 보상 조건: 처치 관여
- 보상: 크레딧 +25, 이동 속도 +20% 2초, 뇌명 집행 제거
- 쿨다운: 15초
- 구현: `thunder.window`, `thunder.hits`, `thunder.active`, `thunder.credits`, 더미 HP 1 도달 시 처치 관여 대체 판정

### 각성

- 분류: 고유 장착 효과
- 조건: 궁극기 R1 사용
- 상태: 8초
- 효과: 공격 속도 +30%, 이동 속도 +12%, 충돌 무시
- 쿨다운: 20초
- 구현: `awakening.active`, `awakening.cooldown`, `attackSpeed`, `effectiveMoveSpeedMps`
- 제한: 충돌 시스템 없음, 충돌 무시는 상태 기록

### 현란함

- 분류: 고유 장착 효과
- 조건: 적 실험체에게 개별 피해 적중
- 상태: 3초 중첩
- 최대 중첩: 8
- 효과: 근거리 중첩당 이동 속도 +3%, 원거리 중첩당 +1.5%
- 구현: `glamour.stacks`, `glamourMoveBonus`, `effectiveMoveSpeedMps`

## 변경 파일

- `index.html`: 추가 효과 버튼, 추가 효과 모달, 캐시 키 `1.2.7`
- `styles.css`: 추가 효과 모달 스타일
- `app.js`: 추가 효과 상태, 타이머, 피해 트리거, 평타 트리거, 속도 반영
- `GPT.md`: 구조 기록
- `CLAUDE.md`: 작업 지침 기록

## 검증

- 구문 검사: `node --check app.js`
- 브라우저 확인: `http://127.0.0.1:8000/index.html?verify=1.2.7`
- 확인 항목: 추가 효과 버튼 1개, 모달 열림, 5개 토글 존재, 캐시 키 2개, 콘솔 오류 0개
- 결과: 통과

## 1.2.8

- 요청: 추가 효과 상태 박스 삭제
- 삭제: `#extraEffectStatus`, `.extra-effect-status`
- 변경: 캐시 키 `1.2.8`
- 변경: 쿨타임 초기화 모드 ON 시 아이템 쿨다운 0초
- 구현: `cooldownResetActive()`, `setExtraEffectCooldown()`, `resetExtraEffectCooldowns()`
- 중첩 방지: 의념 단일 충전, 각성 active 중 재발동 차단, 뇌명 집행 active 중 재발동 차단
- 검증: `node --check app.js`

## 1.2.9

- 요청: 쿨다운 관련 효과 보유 아이템만 유지
- 삭제: 현란함
- 삭제: `effectGlamour`, `glamour.stacks`, `glamourMoveBonus`
- 유지: 의념, 충전 - 섬광, 뇌명 집행, 각성
- 변경: 캐시 키 `1.2.9`
- 조정: 쿨타임 초기화 모드 ON 시 아이템 쿨다운 매 프레임 0초 유지
- 조정: `setExtraEffectCooldown()` 경유, 신규 아이템 쿨다운 즉시 0초
- 중첩 방지: active 상태 재발동 차단, 단일 armed 상태 유지
- 검증: `node --check app.js`

## 1.2.10

- 요청: 효과 hover 설명
- 추가: `.extra-effect-option`, `.extra-effect-desc`
- 표시 조건: hover, focus-within
- 요청: 아이템 효과 발동 시각화
- 추가: `spawnItemEffect(point, kind, size)`
- 추가: `.fx-item`, `.fx-item-willpower`, `.fx-item-flash`, `.fx-item-thunder`, `.fx-item-awakening`
- 추가: `@keyframes itemBurst`
- 발동 지점: 의념 충전, 의념 적중, 충전 - 섬광 적중, 뇌명 집행 발동, 뇌명 집행 처치 관여, 각성 발동
- 변경: 캐시 키 `1.2.10`
- 검증: `node --check app.js`

## 1.2.11

- 요청: 현란함 복구
- 복구: `effectGlamour`, `glamour.stacks`, `glamourMoveBonus`
- 효과: 3초 중첩, 최대 8중첩, 근거리 +3%, 원거리 +1.5%
- 요청: 충전 - 섬광 삭제
- 삭제: `effectFlash`, `flash.cooldown`, `characterLevel`, `dummy.flashSlow`, `.fx-item-flash`
- 요청: 아이템 효과 적용 금색 로그
- 추가: `.log-line.item`
- 변경: 의념, 현란함, 뇌명 집행, 각성 적용 로그 tone `item`
- 추가: `.fx-item-glamour`
- 변경: 캐시 키 `1.2.11`
- 검증: `node --check app.js`

## 1.2.12

- 요청: 공속 변경 아이템 효과 실시간 적용
- 수정: 최종 공격 속도 = 유저 설정 `#attackSpeed` × 아이템 공속 배율
- 추가: `attackSpeedBase()`, `attackSpeedMultiplier()`, `attackInterval()`
- 추가: `syncBasicAttackCooldown()`
- 변경: 평타 쿨다운 남은 비율 보존 후 실시간 재계산
- 변경: 속도 표시 `이속` + `공속`
- 요청: 더미 좌우 이동 기능
- 추가: `#dummyStrafeMode`
- 추가: `DUMMY_STRAFE_RANGE`, `DUMMY_STRAFE_SPEED`
- 추가: `setDummyStrafeAnchor()`, `ensureDummyStrafeAnchor()`
- 동작: 더미 현재 위치 중심, 좌우 3m 왕복
- 우선순위: 더미 좌우 이동 ON > 기존 더미 이동
- 변경: 캐시 키 `1.2.12`
- 검증: `node --check app.js`

## 1.2.13

- 요청: 더미 이동 가속도 일정화
- 요청: 유저와 같은 이동속도
- 요청: 더미 이동, 더미 좌우 이동 통합
- 삭제: `#dummyStrafeMode`
- 삭제: 기존 랜덤 바운스 더미 이동
- 변경: `#dummyMoveMode` 단일 토글
- 변경: 더미 현재 위치 중심 좌우 3m 등속 왕복
- 변경: 더미 이동 속도 = `#moveSpeed` 입력값
- 추가: `DUMMY_MOVE_RANGE`
- 추가: `setDummyMoveAnchor()`, `ensureDummyMoveAnchor()`, `reflectDummyOffset()`
- 변경: 캐시 키 `1.2.13`
- 검증: `node --check app.js`

## 1.2.14

- 요청: 옵션 버튼 추가
- 요청: 조작키 변경처럼 설정 창 표시
- 추가: `#optionsBtn`
- 추가: `#optionsModal`, `#optionsModalClose`
- 이동: 마우스 방향 보기
- 이동: 카메라 잠금
- 이동: 스킬 사거리 표시
- 이동: 쿨타임 초기화
- 이동: 스킬 후 자동 공격
- 이동: 더미 좌우 이동
- 이동: 카메라 흔들림
- 이동: 음성 대사
- 이동: 미안해 알렉스
- 변경: 사이드 패널 직접 토글 제거, 옵션 창 단일 관리
- 추가: `.options-modal`, `.option-settings`
- 변경: 캐시 키 `1.2.14`
- 검증: `node --check app.js`

## 1.2.15

- 요청: R1타 적중 전하 +1
- 요청: R2타 적중 전하 +1
- 순서: R2 적중 전하 +1 → 기존 풀차징 처리
- 변경: R1 `outer.length > 0` 시 `addCharge(1, "R")`
- 변경: R2 `castSecondLightning()` 적중 시 `addCharge(1, "R")`
- 유지: R2 기존 `state.charge = MAX_CHARGE`, `enterOvercharge(..., force, preserveW)`
- 변경: 캐시 키 `1.2.15`
- 검증: `node --check app.js`

## 1.2.16

- 요청: A 스마트 캐스팅 옵션
- 추가: `#aSmartCastMode`
- 추가: `state.aSmartCasting`
- 동작: 옵션 ON, A 키다운 사거리 표시
- 동작: 옵션 ON, A 키업 사거리 표시 종료
- 유지: A 표시 중 좌클릭 평타 동작
- 유지: 옵션 OFF, 기존 A 사거리 유지 표시
- 변경: 옵션 토글 목록에 A 스마트 캐스팅 추가
- 변경: 캐시 키 `1.2.16`
- 검증: `node --check app.js`

## 1.2.17

- 요청: R2 적중 풀충전 즉시 과전하 진입 제거
- 기준: 기존 전하 5스택 과전하 전환 조건
- 변경: 일반 상태 R2 적중 `addCharge(1, "R")` 후 강제 `state.charge = MAX_CHARGE` 제거
- 변경: 일반 상태 R2 적중 `enterOvercharge(..., force)` 제거
- 유지: 전하 5스택 후 `enemyNearby()` false 시 `updateTimers`에서 과전하 진입
- 유지: 과전하 중 수동 R2 적중 재진입, `preserveW`
- 변경: 캐시 키 `1.2.17`
- 검증: `node --check app.js`

## 1.2.18

- 요청: R2 적중 전하 +1 후 풀충전
- 요청: 풀충전 후 기존 패시브 조건 만족 시 과전하
- 추가: `fillCharge()`
- 순서: `castSecondLightning()` 전하 +1 → `fillCharge("전하 최대")`
- 변경: 일반 상태 R2 적중 시 풀충전만 적용
- 유지: 즉시 강제 과전하 진입 없음
- 유지: `updateTimers` 기존 `enemyNearby()` 조건 전환
- 변경: 캐시 키 `1.2.18`
- 검증: `node --check app.js`

## 1.2.19

- 요청: R2 전하 +1 후 풀충전 0.1초 틈
- 추가: `R2_FULL_CHARGE_DELAY = 0.1`
- 추가: `state.pendingR2FullCharge`
- 변경: 일반 상태 R2 적중 시 즉시 `fillCharge()` 대신 지연 예약
- 변경: `updateTimers`에서 지연 만료 후 `fillCharge("전하 최대")`
- 변경: 지연 중 기존 패시브 과전하 전환 차단
- 유지: 지연 만료 후 `enemyNearby()` 조건 만족 시 과전하 전환
- 변경: 캐시 키 `1.2.19`
- 검증: `node --check app.js`
